% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_params.R
\name{shg_load_params}
\alias{shg_load_params}
\title{Load SHG smoking and mortality parameter bundles and configure the instance}
\usage{
shg_load_params(
  shg,
  smoking_url = NULL,
  mortality_url = NULL,
  mort_params_type = c("acm", "ocm")
)
}
\arguments{
\item{shg}{An \code{SHGInterface} instance.}

\item{smoking_url}{URL or local path to the smoking \code{.zip} bundle.}

\item{mortality_url}{URL or local path to the mortality \code{.zip} bundle.}

\item{mort_params_type}{\code{"acm"} (\strong{default}) or \code{"ocm"}.

For private GitHub-hosted zips, set \code{GITHUB_PAT} before downloading.}
}
\value{
The \code{SHGInterface} instance, invisibly.
}
\description{
Downloads (or reuses locally cached copies of) separate \strong{shg-params} smoking
and mortality release zips, merges them into an engine layout under the cache,
and sets \code{input_data_folder} plus relative input filenames on the
\code{SHGInterface} instance.

Each zip uses the \strong{shg-params} release layout (\verb{params/} CSVs plus
\code{metadata.yml}). The simulator expects \verb{smoking/*.csv} and \verb{mortality/*.csv}
under one folder; this function materializes that tree from the two zips.
}
\section{Download timeouts}{

Options \code{shg.params.download.timeout_sec} (default 600) and
\code{shg.params.download.connect_sec} (default 60) control HTTPS transfers
when \pkg{httr2} is installed.
}

