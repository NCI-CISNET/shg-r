% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shg_config.R
\name{shg_load_config}
\alias{shg_load_config}
\alias{shg_use_config_bundle}
\title{Load engine state and parameters from a YAML config file}
\usage{
shg_load_config(shg, path)

shg_use_config_bundle(shg, path)
}
\arguments{
\item{shg}{An \code{SHGInterface} instance.}

\item{path}{Path to a YAML file produced by \code{\link{shg_save_config}} or
compatible hand-written YAML.}
}
\value{
The parsed config list (same object to pass to \code{\link{shg_run}} /
  \code{runSim}). Return value is visible so you can assign:
  \code{config <- shg_load_config(shg, "my-run.yml")}.
}
\description{
Reads the YAML file, applies engine settings with \code{useConfig()}, then
restores parameter tables via \code{\link{shg_load_params}} when the cache is
missing or stale (using \code{params_bundle_source} and \code{params_mortality}
stored in the file).
}
\details{
Private GitHub downloads use the \code{GITHUB_PAT} environment variable when
needed (same as \code{\link{shg_load_params}}).
}
\seealso{
\code{\link{shg_save_config}}, \code{\link{shg_run}}
}
