# TODO: eventually rename to Makevars and use this for MacOS compiling. Currently it causes the build to fail.
CXX_STD = CXX11
#PKG_CXXFLAGS = -I. -DGMP
#PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) -lgm
CXXFLAGS = -g -O0
