#ifndef SHG_VERSION_H
#define SHG_VERSION_H

// Simulation engine build id (legacy XML <VERSION> and get_shg_core_version()).
// Bump when engine behavior changes; not the CRAN package version (see DESCRIPTION).
#define SHG_CORE_VERSION "6.5.3"

#endif // SHG_VERSION_H

